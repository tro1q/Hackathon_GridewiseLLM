# Copy this file to `.env` and fill in real values.
# When ANTHROPIC_API_KEY is empty or "dummy_key", the service runs in
# deterministic NLP fallback mode (still produces valid directives).

ANTHROPIC_API_KEY=
PORT=8080
